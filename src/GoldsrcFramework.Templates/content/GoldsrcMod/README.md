# My GoldSrc Mod

This project was created with the GoldsrcFramework mod template.

## Local setup

Copy `Directory.Build.props.user.example` to `Directory.Build.props.user` and set `HalfLifeInstallDir` to your Half-Life installation path.

```powershell
dotnet build
dotnet build /t:Deploy
```
